# ScheduledConfig
NotNull.scheduledConfig.key=Every configuration entry needs a key.
NotBlank.scheduledConfig.key=The configuration entry needs a key that must not be blank.
NotNull.scheduledConfig.validFrom=Every configuration entry needs a "valid from" timestamp.

